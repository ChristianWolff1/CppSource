/** Definitions of Windows Winsock socket. 
 * Also, put the flags to compile in Microsoft VS C++ or MinGW.
 */
#if defined(_WIN32)
#ifndef _WIN32_WINNT
#define _WIN32_WINNT 0x0600
#endif
#include <winsock2.h>
#include <ws2tcpip.h>
#pragma comment(lib, "ws2_32.lib")


/** Definition of Berkeley (BSD) sockets to run in Linux and MacOS environments. */
#else
#include <sys/types.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <netdb.h>
#include <unistd.h>
#include <errno.h>

#endif
